name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Testes Python
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Rodar testes
        env:
          SECRET_KEY: ci-test-secret-key
          DATABASE_URL: sqlite:///./data/test.db
          ADMIN_EMAIL: admin@test.com
          ADMIN_PASSWORD: testpassword123
        run: pytest tests/ -v

      - name: Validar migrations
        env:
          DATABASE_URL: sqlite:///./data/ci.db
        run: python scripts/migrate.py

  docker-build:
    name: Validar build Docker
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: Build da imagem
        run: docker build -t meufinanceiro:ci .

      - name: Validar inicialização da imagem
        run: |
          docker run -d --name meufinanceiro-ci \
            -e APP_ENV=production \
            -e SECRET_KEY=ci-test-secret-key-with-at-least-32-characters \
            -e ADMIN_EMAIL=admin@test.com \
            -e ADMIN_PASSWORD=testpassword123 \
            -e RUN_SCHEDULER=false \
            -p 8000:8000 meufinanceiro:ci
          sleep 8
          curl -f http://localhost:8000/healthz
          docker stop meufinanceiro-ci

  docker-push:
    name: Publicar imagem no GHCR
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Login no GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extrair metadados da imagem
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/ulyssesrocharosa/meufinanceiro
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{raw}}
            type=sha,prefix=

      - name: Build e push da imagem
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
